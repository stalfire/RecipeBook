<%= form_for @recipe do |f| %>
Title<%= f.text_field :title, type: 'title' %>
Description<%= f.text_field :description, type: 'description' %>
<%= f.hidden_field :user_id, :value => current_user.id %>
<%= f.submit %>
<%end%>
<%= link_to "Back", user_path(current_user) %>